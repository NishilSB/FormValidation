import { CustomeMobInputDirective } from './custome-mob-input.directive';

describe('CustomeMobInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomeMobInputDirective();
    expect(directive).toBeTruthy();
  });
});
